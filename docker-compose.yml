version: '3.5'
services:
  app:
    container_name: cliqz-privacy-warning
    build: .
    volumes:
      - '.:/app'
      - '/app/node_modules'
    environment:
      - NODE_ENV=development
    command: npm start
