version: '3.5'

services:
  app:
    container_name: cliqz-privacy-warning
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '8080:8080'
    environment:
      - NODE_ENV=development
