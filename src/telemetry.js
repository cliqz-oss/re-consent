export default telemetry = (
  action,
  options,
) => {
  /*
  action: The name of the action, e.g. `Link clicked`.
  options: A dictionary which can contain the following options:
    - type: Wheather it's Google, FB or IAB.
    - site: The url of the current page.
    - writeable: If we are able to change this consent.
    - allowed: Number of purposes allowed.
  */
};
