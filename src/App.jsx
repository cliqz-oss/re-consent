import React from 'react';

import Page from './components/Page';

const App = ({ site, face, locationSharing }) => (
  <Page site={site} face={face} locationSharing={locationSharing} />
);

export default App;
